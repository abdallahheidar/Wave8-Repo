/*
 * main.c
 *
 *  Created on: Mar 2, 2020
 *      Author: Youssef Harby
 */


#include "BCM.h"

int main(void)
{
	uint8_t Buffer[20];
	strBcmConfig_t cfg = {
			BCM_UART_ID,
			BCM_RECEIVE
	};

	BCM_Init(&cfg);

	sei();

	BCM_SetupRxBuffer(BCM_UART_ID, (uint8_t*)Buffer, 20);

	while(1)
	{
		BCM_RxDispatch();
		PORTA_DIR = Buffer[19];
	}
}
